<!doctype html>
<html lang="<%= site.locale %>">
  <head>
    <%= render "head", metadata: site.metadata, title: data.title %>
    
    <link rel="stylesheet" href="<%= relative_url '/_bridgetown/static/css/output.css' %>">
    <script src="<%= relative_url '/_bridgetown/static/js/app.js' %>" defer></script>





    <%= render "header", metadata: site.metadata %>
  </head>
  <body class="<%= data.layout %> <%= data.page_class %>">
 
    <%= render "navbar", metadata: site.metadata, resource: resource %>

    <main>
      <%= content %>
    </main>

    <%= render "footer", metadata: site.metadata %>
  </body>
</html>